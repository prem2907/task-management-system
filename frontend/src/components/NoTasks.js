import React from 'react';

const NoTasks = () => {
    return (
        <div>
            <h2>No Tasks Assigned</h2>
            <p>Welcome! It looks like you don't have any tasks assigned yet.</p>
            <p>You can create new tasks once you're ready!</p>
        </div>
    );
};

export default NoTasks;